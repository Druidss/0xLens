# 0xLens 设计规范 / Design Specification

**版本 / Version:** v1.0
**创建日期 / Created:** 2025-11-12
**设计师 / Designer:** Claude (Senior UX/UI Designer)
**技术栈 / Tech Stack:** React 18 + Vite + shadcn/ui + Tailwind CSS

---

## 📋 目录 / Table of Contents

1. [设计规范](#1-设计规范)
2. [交互流程图](#2-交互流程图)
3. [组件标准](#3-组件标准)
4. [页面交互文档](#4-页面交互文档)
5. [设计稿](#5-设计稿)

---

## 1. 设计规范

### 1.1 色彩系统 / Color System

```css
/* 主色调 / Primary (霓虹青色) */
--color-primary: #00f0ff;
--color-primary-dark: #00c0cc;
--color-primary-light: #33f6ff;

/* 辅助色 / Secondary (紫色) */
--color-secondary: #7b61ff;

/* 背景色 / Background */
--bg-primary: #0a0e27;
--bg-secondary: #141b3d;
--bg-card: rgba(20, 27, 61, 0.7);

/* 文字色 / Text */
--text-primary: #e8e8e8;
--text-secondary: #a8adc7;
--text-muted: #6b7280;

/* 功能色 / Functional */
--color-success: #00ff87;    /* 做多 / Long */
--color-error: #ff3864;      /* 做空 / Short */
--color-warning: #ffd600;
--color-info: #00d4ff;

/* 边框 / Border */
--border-default: rgba(0, 240, 255, 0.2);
--border-focus: rgba(0, 240, 255, 0.6);

/* 阴影 / Shadow */
--shadow-sm: 0 2px 4px rgba(0, 0, 0, 0.08);
--shadow-md: 0 4px 8px rgba(0, 0, 0, 0.12);
--shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.16);
--glow-primary: 0 0 20px rgba(0, 240, 255, 0.5);
```

### 1.2 字体系统 / Typography

```css
/* 字体家族 / Font Family */
--font-sans: 'Inter', -apple-system, sans-serif;
--font-mono: 'JetBrains Mono', monospace;

/* 字号 / Font Size */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
--text-4xl: 2.25rem;   /* 36px */

/* 字重 / Font Weight */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

### 1.3 间距系统 / Spacing

```css
--spacing-2: 0.5rem;    /* 8px */
--spacing-4: 1rem;      /* 16px */
--spacing-6: 1.5rem;    /* 24px */
--spacing-8: 2rem;      /* 32px */
--spacing-12: 3rem;     /* 48px */
```

### 1.4 圆角与动画 / Radius & Animation

```css
/* 圆角 / Border Radius */
--radius-sm: 0.25rem;   /* 4px */
--radius-md: 0.5rem;    /* 8px */
--radius-lg: 0.75rem;   /* 12px */
--radius-full: 9999px;

/* 动画 / Animation */
--duration-fast: 150ms;
--duration-normal: 250ms;
--ease-out: cubic-bezier(0, 0, 0.2, 1);
```

### 1.5 Web3 特殊效果 / Web3 Effects

```css
/* 毛玻璃卡片 / Glass Card */
.glass-card {
  background: var(--bg-card);
  backdrop-filter: blur(16px) saturate(180%);
  border: 1px solid var(--border-default);
  box-shadow:
    var(--shadow-md),
    0 0 0 1px rgba(0, 240, 255, 0.1) inset,
    0 0 20px rgba(0, 240, 255, 0.15);
}

.glass-card:hover {
  border-color: rgba(0, 240, 255, 0.4);
  box-shadow:
    var(--shadow-lg),
    0 0 30px rgba(0, 240, 255, 0.3);
}

/* 霓虹文字 / Neon Text */
.neon-text {
  color: var(--color-primary);
  text-shadow: 0 0 10px rgba(0, 240, 255, 0.6);
}

/* 渐变文字 / Gradient Text */
.gradient-text {
  background: linear-gradient(135deg, #00f0ff 0%, #7b61ff 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
```

---

## 2. 交互流程图

### 2.1 用户旅程地图 / User Journey Map

```
[用户访问 0xLens]
       ↓
[首页 / Homepage]
  - 显示最新 AI 摘要
  - 展示核心数据卡片
       ↓
   ┌───┴───┬──────────┬───────────┐
   ↓       ↓          ↓           ↓
[AI摘要] [鲸鱼追踪] [交易员观点] [策略编辑器]
   │       │          │           │
   │       │          │           │
   ↓       ↓          ↓           ↓
[查看详情] [实时监控] [共识分析] [自定义策略]
   │       │          │           │
   └───┬───┴──────────┴───────────┘
       ↓
[做出交易决策]
       ↓
[返回首页继续监控]
```

### 2.2 AI 摘要功能流程 / AI Digest Flow

```
用户操作              系统响应              页面状态
   │                    │                    │
[进入首页] ────────→ [加载最新摘要] ─────→ [显示骨架屏]
   │                    │                    │
   │                    ↓                    │
   │              [获取成功] ───────────→ [展示 AI 摘要卡片]
   │                    │                    │
   │                    │                    ↓
   │                    │              [交易员共识: 65%]
   │                    │              [关键价位显示]
   │                    │              [AI 建议: 做多]
   │                    │                    │
[点击"查看详情"] ─→ [展开详细分析] ─────→ [显示更多内容]
   │                    │                    │
   │                    │              - 交易员列表
   │                    │              - 鲸鱼动向
   │                    │              - 自定义策略结果
```

### 2.3 策略编辑器流程 / Strategy Editor Flow

```
[用户点击"策略编辑器"]
         ↓
  ┌──────┴──────┐
  │             │
[Pine Script]  [自然语言]
  │             │
  ↓             ↓
[输入代码]    [输入描述]
  │             │
  └──────┬──────┘
         ↓
   [GPT-4 解析]
         ↓
    ┌────┴────┐
    │         │
 [成功]    [失败]
    │         │
    ↓         ↓
[显示策略] [显示错误提示]
    │         │
    ↓         │
[运行回测] ←──┘
    │
    ↓
[显示回测结果]
  - 胜率
  - 盈亏比
  - 最大回撤
    │
    ↓
[保存策略]
```

---

## 3. 组件标准

### 3.1 shadcn/ui 组件安装清单 / Component Installation List

```bash
# 基础组件 / Basic Components
npx shadcn-ui@latest add button
npx shadcn-ui@latest add input
npx shadcn-ui@latest add textarea
npx shadcn-ui@latest add card
npx shadcn-ui@latest add badge
npx shadcn-ui@latest add avatar

# 表单组件 / Form Components
npx shadcn-ui@latest add form
npx shadcn-ui@latest add label
npx shadcn-ui@latest add select
npx shadcn-ui@latest add checkbox

# 反馈组件 / Feedback Components
npx shadcn-ui@latest add toast
npx shadcn-ui@latest add alert
npx shadcn-ui@latest add progress
npx shadcn-ui@latest add skeleton

# 覆盖层组件 / Overlay Components
npx shadcn-ui@latest add dialog
npx shadcn-ui@latest add dropdown-menu
npx shadcn-ui@latest add popover

# 数据展示 / Data Display
npx shadcn-ui@latest add table
npx shadcn-ui@latest add tabs
```

### 3.2 Button 组件规范 / Button Specification

```tsx
// 主要按钮 / Primary Button
<Button variant="default" size="default" className="neon-border">
  提交 / Submit
</Button>

// 次要按钮 / Secondary Button
<Button variant="outline" size="default">
  取消 / Cancel
</Button>

// 做多按钮 / Long Button
<Button className="bg-[var(--color-success)] hover:bg-[var(--color-success)]/90">
  做多 / Long
</Button>

// 做空按钮 / Short Button
<Button className="bg-[var(--color-error)] hover:bg-[var(--color-error)]/90">
  做空 / Short
</Button>
```

**尺寸规范 / Size Specs:**

| Size | 高度 / Height | 内边距 / Padding | 字号 / Font Size |
|---|---|---|---|
| sm | 32px | 12px 16px | text-sm (14px) |
| default | 40px | 16px 24px | text-base (16px) |
| lg | 48px | 20px 32px | text-lg (18px) |

### 3.3 Card 组件规范 / Card Specification

```tsx
// AI 摘要卡片 / AI Digest Card
<Card className="glass-card">
  <CardHeader>
    <CardTitle className="text-2xl gradient-text">
      🤖 AI 每日摘要 / Daily AI Digest
    </CardTitle>
    <CardDescription className="text-muted">
      更新时间 / Updated: 08:00 UTC
    </CardDescription>
  </CardHeader>
  <CardContent>
    {/* 内容区域 */}
  </CardContent>
</Card>
```

### 3.4 自定义业务组件 / Custom Business Components

#### 3.4.1 ConsensusGauge (共识度仪表盘)

```tsx
interface ConsensusGaugeProps {
  value: number;        // 0-100
  label: string;
}

function ConsensusGauge({ value, label }: ConsensusGaugeProps) {
  const color = value > 60 ? 'success' : value < 40 ? 'error' : 'warning';

  return (
    <div className="space-y-2">
      <div className="flex justify-between text-sm">
        <span className="text-secondary">{label}</span>
        <span className="font-semibold text-primary">{value}%</span>
      </div>
      <div className="h-2 bg-bg-tertiary rounded-full overflow-hidden">
        <div
          className={`h-full rounded-full transition-all duration-500 bg-[var(--color-${color})]`}
          style={{ width: `${value}%` }}
        />
      </div>
      <p className="text-xs text-muted">
        {value > 60 ? '市场偏多 / Bullish' :
         value < 40 ? '市场偏空 / Bearish' :
         '市场分歧 / Neutral'}
      </p>
    </div>
  );
}
```

#### 3.4.2 TraderOpinionCard (交易员观点卡片)

```tsx
interface TraderOpinionCardProps {
  name: string;
  avatar: string;
  opinion: 'bullish' | 'bearish' | 'neutral';
  logic: string;
  confidence: number;
  timeframe: string;
}

function TraderOpinionCard({ name, avatar, opinion, logic, confidence, timeframe }: TraderOpinionCardProps) {
  const opinionColor = {
    bullish: 'var(--color-success)',
    bearish: 'var(--color-error)',
    neutral: 'var(--color-warning)'
  }[opinion];

  return (
    <Card className="glass-card hover:border-primary/40 transition-all">
      <CardHeader>
        <div className="flex items-center gap-3">
          <Avatar>
            <img src={avatar} alt={name} />
          </Avatar>
          <div className="flex-1">
            <p className="font-medium">{name}</p>
            <p className="text-xs text-muted">{timeframe}</p>
          </div>
          <Badge style={{ backgroundColor: opinionColor }}>
            {opinion === 'bullish' ? '看多' : opinion === 'bearish' ? '看空' : '中性'}
          </Badge>
        </div>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-secondary mb-2">
          <strong>交易逻辑 / Logic:</strong> {logic}
        </p>
        <div className="flex items-center gap-2 text-xs">
          <span className="text-muted">置信度 / Confidence:</span>
          <Progress value={confidence} className="flex-1" />
          <span className="font-semibold text-primary">{confidence}%</span>
        </div>
      </CardContent>
    </Card>
  );
}
```

#### 3.4.3 WhaleTrackerPanel (鲸鱼追踪面板)

```tsx
interface WhaleOrder {
  wallet: string;
  type: 'bid' | 'ask';
  price: string;
  size: string;
  timestamp: string;
}

function WhaleTrackerPanel({ orders }: { orders: WhaleOrder[] }) {
  return (
    <Card className="glass-card">
      <CardHeader>
        <CardTitle className="flex items-center gap-2">
          <span>🐋</span>
          <span>鲸鱼追踪 / Whale Tracking</span>
        </CardTitle>
      </CardHeader>
      <CardContent>
        <div className="space-y-3">
          {orders.map((order, i) => (
            <div
              key={i}
              className={`p-3 rounded-lg border ${
                order.type === 'bid'
                  ? 'bg-success/10 border-success/30'
                  : 'bg-error/10 border-error/30'
              }`}
            >
              <div className="flex justify-between items-start mb-2">
                <div>
                  <Badge variant="outline" className="mb-1">
                    {order.type === 'bid' ? '买单 / Bid' : '卖单 / Ask'}
                  </Badge>
                  <p className="text-xs text-muted font-mono">
                    {order.wallet.slice(0, 8)}...
                  </p>
                </div>
                <div className="text-right">
                  <p className="text-lg font-bold font-mono">{order.price}</p>
                  <p className="text-sm text-secondary">{order.size}</p>
                </div>
              </div>
              <p className="text-xs text-muted">{order.timestamp}</p>
            </div>
          ))}
        </div>
      </CardContent>
    </Card>
  );
}
```

---

## 4. 页面交互文档

### 4.1 首页 (Homepage) 交互说明

**页面路径 / Path:** `/`

**页面用途 / Purpose:**
展示最新的 AI 行情摘要、交易员共识、鲸鱼动向等核心信息，帮助用户快速做出交易决策。

#### 4.1.1 页面状态定义 / Page States

| 状态 / State | 触发条件 / Trigger | 视觉表现 / Visual | 用户操作 / Actions |
|---|---|---|---|
| **初始加载 / Loading** | 页面首次加载 | 显示 3 个骨架屏卡片 | 无 |
| **加载完成 / Loaded** | 数据获取成功 | 显示完整内容 | 所有交互可用 |
| **空状态 / Empty** | 无最新数据 | 显示"暂无数据"提示 | 提供刷新按钮 |
| **错误状态 / Error** | API 请求失败 | 显示错误提示 + 重试按钮 | 点击重试 |
| **刷新中 / Refreshing** | 用户手动刷新 | 顶部显示加载条 | 禁用刷新按钮 |

#### 4.1.2 核心交互元素 / Key Interactive Elements

##### 元素 1: AI 摘要卡片 / AI Digest Card

**类型 / Type:** Card (可展开)

**默认状态 / Default State:**
```
┌────────────────────────────────────────┐
│  🤖 AI 每日摘要             08:00 UTC   │
├────────────────────────────────────────┤
│                                        │
│  交易员共识: 偏多 ████████░░ 65%       │
│                                        │
│  关键价位:                              │
│  📈 阻力: $45,200 / $46,500            │
│  📉 支撑: $43,800 / $42,000            │
│                                        │
│  AI 建议: 轻仓做多 ✅ (72%)             │
│  💰 仓位: 20%  🎯 入场: $43,800         │
│  🛑 止损: $42,500                       │
│                                        │
│  [查看详细分析 →]                       │
└────────────────────────────────────────┘
```

**交互行为 / Interaction:**

```
用户操作         视觉反馈              系统响应
   │                │                    │
[鼠标悬停] ──→ [卡片边框发光]           无
   │         [shadow-lg + glow]          │
   │                │                    │
[点击卡片] ────→ [卡片高度增加] ─────→ [展开详细内容]
   │         [height: auto]              │
   │                │                    │
   │                ↓                    ↓
   │         [显示展开动画]      [加载更多数据]
   │         [slide-down 250ms]          │
   │                │                    │
   │                ↓                    ↓
   │         [显示详细内容]      [显示鲸鱼动向]
   │         - 交易员列表               [显示策略结果]
   │         - 分歧点分析               [显示历史准确率]
```

**微交互设计 / Micro-interaction:**

```css
/* 卡片悬停动画 / Card Hover */
.ai-digest-card {
  transition: all 250ms var(--ease-out);
}

.ai-digest-card:hover {
  transform: translateY(-4px);
  border-color: rgba(0, 240, 255, 0.4);
  box-shadow:
    var(--shadow-lg),
    0 0 30px rgba(0, 240, 255, 0.3);
}
```

##### 元素 2: 共识度仪表盘 / Consensus Gauge

**交互行为 / Interaction:**

```
初始状态: 进度条 0%
    ↓
[页面加载完成]
    ↓
[动画填充到实际值]
  (500ms, ease-out)
    ↓
最终状态: 进度条 65%
```

**动画代码 / Animation:**

```tsx
useEffect(() => {
  // 延迟动画，让用户看到填充效果
  setTimeout(() => {
    setDisplayValue(actualValue);
  }, 100);
}, [actualValue]);

<div
  className="transition-all duration-500 ease-out"
  style={{ width: `${displayValue}%` }}
/>
```

##### 元素 3: 刷新按钮 / Refresh Button

**位置 / Position:** 页面右上角

**交互行为 / Interaction:**

```
[点击刷新按钮]
      ↓
[按钮图标旋转 360°]
  (800ms, linear)
      ↓
[禁用按钮]
[opacity: 0.5]
      ↓
[调用 API 获取最新数据]
      ↓
  ┌───┴───┐
  │       │
[成功]  [失败]
  │       │
  ↓       ↓
[显示  [显示错误
 toast   Toast]
 提示]     │
  │       │
  └───┬───┘
      ↓
[恢复按钮可用状态]
```

#### 4.1.3 响应式行为 / Responsive Behavior

**桌面端 (≥1024px):**
- 卡片 3 列布局
- 每个卡片宽度 33.33%
- 间距 24px

**平板端 (640px - 1023px):**
- 卡片 2 列布局
- 每个卡片宽度 50%
- 间距 16px

**移动端 (<640px):**
- 卡片单列布局
- 卡片宽度 100%
- 间距 12px
- 字号缩小一级

---

### 4.2 策略编辑器页面交互说明

**页面路径 / Path:** `/strategy-editor`

#### 4.2.1 页面布局 / Layout

```
┌─────────────────────────────────────────────────┐
│  [← 返回]  策略编辑器 / Strategy Editor          │
├─────────────────────────────────────────────────┤
│                                                 │
│  [Pine Script]  [自然语言]  ← 标签切换           │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │                                           │ │
│  │  // 在此输入 Pine Script 代码              │ │
│  │  // Enter Pine Script code here          │ │
│  │                                           │ │
│  │  strategy("My Strategy", overlay=true)    │ │
│  │                                           │ │
│  │  ▊ ← 光标                                  │ │
│  │                                           │ │
│  └───────────────────────────────────────────┘ │
│                                                 │
│  [解析策略]  [运行回测]                          │
│                                                 │
├─────────────────────────────────────────────────┤
│  回测结果 / Backtest Results                     │
│                                                 │
│  ✅ 胜率 Win Rate: 62%                          │
│  📊 盈亏比 Risk/Reward: 1:1.8                   │
│  📉 最大回撤 Max Drawdown: 8.5%                 │
│                                                 │
│  [保存策略]  [应用到 AI 摘要]                    │
└─────────────────────────────────────────────────┘
```

#### 4.2.2 表单验证交互 / Form Validation

**验证时机 / Validation Timing:**
- 用户点击"解析策略"按钮时进行验证
- 如果是自然语言，调用 GPT-4 解析
- 如果是 Pine Script，先语法检查，再解析

**错误提示 / Error Display:**

```tsx
{errors.strategy && (
  <Alert variant="destructive" className="mt-2">
    <AlertTitle>策略解析失败 / Parse Failed</AlertTitle>
    <AlertDescription>{errors.strategy.message}</AlertDescription>
  </Alert>
)}
```

---

## 5. 设计稿

### 5.1 首页设计稿 (桌面端) / Homepage Desktop

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  [0xLens Logo]              [AI摘要] [鲸鱼追踪] [交易员] [策略]  [🔔] │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                                                                │ │
│  │         0xLens - AI 驱动的 BTC 交易情报平台                      │ │
│  │         AI-Powered BTC Trading Intelligence Platform          │ │
│  │                                                                │ │
│  │         实时监控 · 智能分析 · 精准建议                           │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │ 🤖 AI 每日摘要  │  │ 🐋 鲸鱼追踪     │  │ 👥 交易员观点   │        │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤        │
│  │                │  │                │  │                │        │
│  │ 交易员共识:     │  │ Top 鲸鱼钱包:   │  │ 共识度:         │        │
│  │ 偏多 ████░ 65% │  │                │  │ ████████░░ 72% │        │
│  │                │  │ 0x123...abc    │  │                │        │
│  │ 关键价位:       │  │ 买单 $2.5M     │  │ 看多: 8人       │        │
│  │ 阻力: $45,200  │  │ @ $44,000      │  │ 看空: 3人       │        │
│  │ 支撑: $43,800  │  │                │  │ 中性: 1人       │        │
│  │                │  │ 0x456...def    │  │                │        │
│  │ AI 建议:       │  │ 卖单 $1.8M     │  │ 分歧点:         │        │
│  │ ✅ 轻仓做多     │  │ @ $45,200      │  │ 60% 认为突破   │        │
│  │    (72%)       │  │                │  │ 40% 认为回调   │        │
│  │                │  │ [查看更多 →]   │  │                │        │
│  │ 仓位: 20%      │  │                │  │ [查看详情 →]   │        │
│  │ 入场: $43,800  │  │                │  │                │        │
│  │ 止损: $42,500  │  │                │  │                │        │
│  │                │  │                │  │                │        │
│  │ [查看详情 →]   │  │                │  │                │        │
│  │                │  │                │  │                │        │
│  └────────────────┘  └────────────────┘  └────────────────┘        │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │                                                              │  │
│  │  📋 您的自定义策略分析 / Your Custom Strategy Analysis        │  │
│  │                                                              │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │                                                      │  │  │
│  │  │  策略名称: 双均线交叉策略 (用户口述)                   │  │  │
│  │  │  Strategy: Dual MA Crossover (User Voice Input)     │  │  │
│  │  │                                                      │  │  │
│  │  │  当前信号: 🟢 做多 / Long (触发: 2025-11-10 14:30)   │  │  │
│  │  │  MA(20): $44,150  |  MA(50): $43,800                │  │  │
│  │  │                                                      │  │  │
│  │  │  回测表现 (过去 30 天):                               │  │  │
│  │  │  ✅ 胜率: 62%  📊 盈亏比: 1:1.8  📉 回撤: 8.5%      │  │  │
│  │  │                                                      │  │  │
│  │  │  AI 个性化建议:                                       │  │  │
│  │  │  根据您的双均线策略，当前处于做多阶段。结合交易员观点   │  │  │
│  │  │  和鲸鱼数据，建议在 $43,800-$44,200 区间加仓。        │  │  │
│  │  │                                                      │  │  │
│  │  │  [编辑策略]  [查看历史表现]                          │  │  │
│  │  │                                                      │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 5.2 首页设计稿 (移动端) / Homepage Mobile

```
┌────────────────────┐
│                    │
│ [≡] 0xLens    [🔔] │
│                    │
├────────────────────┤
│                    │
│  0xLens            │
│  AI 交易情报平台     │
│                    │
├────────────────────┤
│                    │
│ ┌────────────────┐ │
│ │ 🤖 AI 每日摘要  │ │
│ ├────────────────┤ │
│ │                │ │
│ │ 交易员共识:     │ │
│ │ 偏多 ████░ 65% │ │
│ │                │ │
│ │ 关键价位:       │ │
│ │ 阻力: $45,200  │ │
│ │ 支撑: $43,800  │ │
│ │                │ │
│ │ AI 建议:       │ │
│ │ ✅ 轻仓做多     │ │
│ │    (72%)       │ │
│ │                │ │
│ │ [查看详情 →]   │ │
│ └────────────────┘ │
│                    │
│ ┌────────────────┐ │
│ │ 🐋 鲸鱼追踪     │ │
│ ├────────────────┤ │
│ │                │ │
│ │ 0x123...abc    │ │
│ │ 买单 $2.5M     │ │
│ │ @ $44,000      │ │
│ │                │ │
│ │ 0x456...def    │ │
│ │ 卖单 $1.8M     │ │
│ │ @ $45,200      │ │
│ │                │ │
│ │ [查看更多 →]   │ │
│ └────────────────┘ │
│                    │
│ ┌────────────────┐ │
│ │ 👥 交易员观点   │ │
│ ├────────────────┤ │
│ │                │ │
│ │ 共识度:         │ │
│ │ ████████░░ 72% │ │
│ │                │ │
│ │ 看多: 8人       │ │
│ │ 看空: 3人       │ │
│ │                │ │
│ │ [查看详情 →]   │ │
│ └────────────────┘ │
│                    │
└────────────────────┘
```

### 5.3 策略编辑器设计稿 (桌面端) / Strategy Editor Desktop

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  [← 返回首页]  策略编辑器 / Strategy Editor                           │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  创建您的自定义交易策略，AI 将基于此策略提供个性化建议。               │
│  Create your custom trading strategy, AI will provide personalized  │
│  suggestions based on it.                                           │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  [Pine Script ●]  [自然语言 ○]  ← 标签切换                           │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  策略代码编辑器 / Strategy Code Editor                         │  │
│  ├──────────────────────────────────────────────────────────────┤  │
│  │                                                              │  │
│  │  1  //@version=5                                             │  │
│  │  2  strategy("双均线交叉", overlay=true)                       │  │
│  │  3                                                            │  │
│  │  4  // 参数设置                                                │  │
│  │  5  fastLength = input.int(20, "快速均线")                     │  │
│  │  6  slowLength = input.int(50, "慢速均线")                     │  │
│  │  7                                                            │  │
│  │  8  // 计算均线                                                │  │
│  │  9  fastMA = ta.sma(close, fastLength)                       │  │
│  │ 10  slowMA = ta.sma(close, slowLength)                       │  │
│  │ 11                                                            │  │
│  │ 12  // 交易逻辑                                                │  │
│  │ 13  if ta.crossover(fastMA, slowMA)                          │  │
│  │ 14      strategy.entry("Long", strategy.long)                │  │
│  │ 15  if ta.crossunder(fastMA, slowMA)                         │  │
│  │ 16      strategy.close("Long")                               │  │
│  │ 17  ▊ ← 光标                                                  │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  [解析策略 🔍]  [运行回测 ▶]  [清空 ✕]                              │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  回测结果 / Backtest Results                                          │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐        │
│  │  ✅ 胜率        │  │  📊 盈亏比      │  │  📉 最大回撤    │        │
│  │  Win Rate      │  │  Risk/Reward   │  │  Max Drawdown  │        │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤        │
│  │                │  │                │  │                │        │
│  │     62%        │  │     1:1.8      │  │     8.5%       │        │
│  │                │  │                │  │                │        │
│  └────────────────┘  └────────────────┘  └────────────────┘        │
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  策略表现曲线 / Strategy Performance Chart                     │  │
│  │                                                              │  │
│  │      ▲                                                       │  │
│  │  20% │                              ╱╲                       │  │
│  │      │                            ╱    ╲                     │  │
│  │  10% │           ╱╲            ╱        ╲                   │  │
│  │      │         ╱    ╲        ╱            ╲                 │  │
│  │   0% ├────────────────────────────────────────► 时间         │  │
│  │      │                                                       │  │
│  │ -10% │                                                       │  │
│  │      └───────────────────────────────────────────────────    │  │
│  │        Day 1      Day 15      Day 30                        │  │
│  │                                                              │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  [保存策略 💾]  [应用到 AI 摘要 ✅]  [导出策略 ↓]                     │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 5.4 组件状态设计示例 / Component States Example

#### Button 组件状态 / Button States

```
Default (默认):
┌─────────────┐
│   提交 Submit   │  ← 背景色 primary, 霓虹边框
└─────────────┘

Hover (悬停):
┌─────────────┐
│   提交 Submit   │  ← 背景加深, 发光增强
└─────────────┘
     ↑ 微微上浮 translateY(-2px)

Active (按下):
┌─────────────┐
│   提交 Submit   │  ← scale(0.98)
└─────────────┘

Loading (加载中):
┌─────────────┐
│  ◐ 提交中...  │  ← Spinner 旋转
└─────────────┘

Disabled (禁用):
┌─────────────┐
│   提交 Submit   │  ← opacity: 0.5, 禁止点击
└─────────────┘
```

#### Card 组件状态 / Card States

```
Default (默认):
┌─────────────────────┐
│  🤖 AI 摘要          │  ← 毛玻璃背景
│                     │     淡青色边框
│  [内容]              │
│                     │
└─────────────────────┘

Hover (悬停):
┌═════════════════════┐
│  🤖 AI 摘要          │  ← 边框加亮
│                     │     外发光效果
│  [内容]              │     轻微上浮
│                     │
└═════════════════════┘
     ↑ translateY(-4px)

Expanded (展开):
┌─────────────────────┐
│  🤖 AI 摘要          │
├─────────────────────┤
│                     │
│  [内容]              │
│                     │
│  ┌───────────────┐  │
│  │ [更多详情]     │  │  ← 展开区域
│  │               │  │     slide-down 动画
│  └───────────────┘  │
│                     │
│  [收起 ▲]           │
└─────────────────────┘

Loading (加载中):
┌─────────────────────┐
│  ▓▓▓▓░░░░░░░░        │  ← 骨架屏
│  ▓▓░░░░░░░░░         │     闪烁动画
│  ▓▓▓▓▓░░░░░░░        │
└─────────────────────┘
```

---

## 6. 设计交付清单 / Design Deliverables Checklist

完成的交付物 / Completed Deliverables:

- [x] ✅ **视觉系统完整定义**（色彩、字体、间距、圆角、阴影、动画）
- [x] ✅ **Design Tokens CSS 变量文件**（可直接用于开发）
- [x] ✅ **交互流程图**（用户旅程 + 核心功能流程 + 策略编辑器流程）
- [x] ✅ **shadcn/ui 组件清单和安装命令**
- [x] ✅ **自定义业务组件设计规范**（ConsensusGauge, TraderOpinionCard, WhaleTrackerPanel）
- [x] ✅ **完整页面的交互文档**（首页 + 策略编辑器，包含所有状态）
- [x] ✅ **桌面端和移动端设计稿**（ASCII art 格式）
- [x] ✅ **响应式断点和适配规则**
- [x] ✅ **微交互设计说明**（动画、过渡效果）
- [x] ✅ **组件状态设计**（Button 和 Card 的所有状态）

---

## 7. 开发实施建议 / Development Implementation Guide

### 7.1 项目初始化 / Project Initialization

```bash
# 1. 创建 Vite + React + TypeScript 项目
npm create vite@latest 0xLens -- --template react-ts
cd 0xLens

# 2. 安装 Tailwind CSS
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

# 3. 安装 shadcn/ui
npx shadcn-ui@latest init

# 4. 安装其他依赖
npm install zustand react-router-dom framer-motion recharts
npm install react-hook-form zod @hookform/resolvers
```

### 7.2 Tailwind 配置 / Tailwind Config

```js
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
export default {
  darkMode: ['class'],
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: '#00f0ff',
          dark: '#00c0cc',
          light: '#33f6ff',
        },
        secondary: '#7b61ff',
        success: '#00ff87',
        error: '#ff3864',
        warning: '#ffd600',
        info: '#00d4ff',
      },
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      animation: {
        'skeleton-shimmer': 'skeleton-shimmer 1.5s ease-in-out infinite',
        'neon-pulse': 'neon-pulse 2s ease-in-out infinite',
      },
      keyframes: {
        'skeleton-shimmer': {
          '0%': { backgroundPosition: '-200% 0' },
          '100%': { backgroundPosition: '200% 0' },
        },
        'neon-pulse': {
          '0%, 100%': { boxShadow: '0 0 20px rgba(0, 240, 255, 0.5)' },
          '50%': { boxShadow: '0 0 30px rgba(0, 240, 255, 0.8)' },
        },
      },
    },
  },
  plugins: [require('tailwindcss-animate')],
}
```

### 7.3 全局样式 / Global Styles

```css
/* src/index.css */
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --color-primary: #00f0ff;
    --bg-primary: #0a0e27;
    --bg-secondary: #141b3d;
    --bg-card: rgba(20, 27, 61, 0.7);
    --text-primary: #e8e8e8;
    --text-secondary: #a8adc7;
    --border-default: rgba(0, 240, 255, 0.2);
    --glow-primary: 0 0 20px rgba(0, 240, 255, 0.5);
  }

  body {
    @apply bg-[var(--bg-primary)] text-[var(--text-primary)];
    font-family: 'Inter', sans-serif;
  }
}

@layer utilities {
  .glass-card {
    background: var(--bg-card);
    backdrop-filter: blur(16px) saturate(180%);
    border: 1px solid var(--border-default);
    box-shadow:
      0 4px 8px rgba(0, 0, 0, 0.12),
      0 0 0 1px rgba(0, 240, 255, 0.1) inset,
      0 0 20px rgba(0, 240, 255, 0.15);
  }

  .glass-card:hover {
    border-color: rgba(0, 240, 255, 0.4);
    box-shadow:
      0 8px 16px rgba(0, 0, 0, 0.16),
      0 0 30px rgba(0, 240, 255, 0.3);
  }

  .neon-text {
    color: var(--color-primary);
    text-shadow: 0 0 10px rgba(0, 240, 255, 0.6);
  }

  .gradient-text {
    background: linear-gradient(135deg, #00f0ff 0%, #7b61ff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
}
```

### 7.4 shadcn/ui 组件安装 / Component Installation

```bash
# 按需安装组件 (根据第 3 章的清单)
npx shadcn-ui@latest add button input textarea card badge avatar
npx shadcn-ui@latest add form label select checkbox
npx shadcn-ui@latest add toast alert progress skeleton
npx shadcn-ui@latest add dialog dropdown-menu popover
npx shadcn-ui@latest add table tabs
```

### 7.5 项目结构建议 / Project Structure

```
0xLens/
├── src/
│   ├── components/
│   │   ├── ui/                    # shadcn/ui 组件
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   └── ...
│   │   ├── business/              # 自定义业务组件
│   │   │   ├── ConsensusGauge.tsx
│   │   │   ├── TraderOpinionCard.tsx
│   │   │   ├── WhaleTrackerPanel.tsx
│   │   │   └── AIDigestCard.tsx
│   │   └── layout/                # 布局组件
│   │       ├── Header.tsx
│   │       └── Footer.tsx
│   ├── pages/
│   │   ├── Home.tsx               # 首页
│   │   ├── StrategyEditor.tsx     # 策略编辑器
│   │   └── ...
│   ├── hooks/                     # 自定义 Hooks
│   ├── lib/                       # 工具函数
│   ├── store/                     # Zustand 状态管理
│   ├── types/                     # TypeScript 类型
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css                  # 全局样式
└── ...
```

---

## 8. 附录 / Appendix

### 8.1 字体引入 / Font Loading

在 `index.html` 中添加：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
```

### 8.2 图标库 / Icon Library

推荐使用 **Lucide React**：

```bash
npm install lucide-react
```

```tsx
import { TrendingUp, TrendingDown, Activity } from 'lucide-react';

<TrendingUp className="w-4 h-4 text-success" />
```

---

**文档完成 / Document Completed** ✅

**设计师签名 / Designer Signature:** Claude (Senior UX/UI Designer)
**文档状态 / Status:** 已完成，可用于开发 / Completed, Ready for Development

---

**使用说明 / Usage Instructions:**

1. 开发团队可以直接使用本文档中的 Design Tokens
2. 所有 CSS 变量可以复制到 `index.css` 中
3. shadcn/ui 组件安装命令可以直接执行
4. 自定义组件代码可以作为开发起点
5. 设计稿提供了清晰的视觉参考

如需调整或补充，请联系设计师。

If adjustments or additions are needed, please contact the designer.
